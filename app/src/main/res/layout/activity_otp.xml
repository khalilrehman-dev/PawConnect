<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F7F7F7">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Verification"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#1A1A1A"
            android:layout_marginTop="48dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Almost there!"
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginTop="6dp"
            android:layout_marginBottom="32dp" />

        <!-- Instruction card — shown for both email and phone -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/card_background"
            android:padding="20dp"
            android:layout_marginBottom="24dp">

            <TextView
                android:id="@+id/tvOtpSentTo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Verification sent"
                android:textSize="14sp"
                android:textColor="#1A1A1A"
                android:lineSpacingExtra="6dp" />

        </LinearLayout>

        <!-- Label above OTP input — visible for phone, hidden for email -->
        <TextView
            android:id="@+id/tvOtpLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enter OTP"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#1A1A1A"
            android:layout_marginBottom="8dp"
            android:visibility="gone" />

        <!-- OTP input — visible for phone, hidden for email -->
        <com.google.android.material.textfield.TextInputLayout
            android:id="@+id/tilOtp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="6-digit OTP"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            app:boxStrokeColor="#1A1A1A"
            app:boxStrokeWidth="1.5dp"
            app:hintTextColor="#1A1A1A"
            android:layout_marginBottom="20dp"
            android:visibility="gone">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/etOtp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="numberPassword"
                android:maxLength="6"
                android:gravity="center"
                android:textSize="24sp"
                android:textColor="#1A1A1A"
                android:letterSpacing="0.5" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- Open Email App — visible for email only -->
        <Button
            android:id="@+id/btnOpenMail"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Open Email App"
            android:textSize="15sp"
            android:backgroundTint="#F0F0F0"
            android:textColor="#1A1A1A"
            android:layout_marginBottom="12dp"
            android:visibility="gone" />

        <!-- Primary action button -->
        <Button
            android:id="@+id/btnVerify"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="Continue"
            android:textSize="15sp"
            android:backgroundTint="#1A1A1A"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="16dp" />

        <!-- Resend -->
        <TextView
            android:id="@+id/tvResend"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Resend in 30s"
            android:textSize="14sp"
            android:textColor="#888888"
            android:layout_gravity="center"
            android:padding="8dp" />

        <!-- Progress -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="8dp"
            android:visibility="gone"
            android:indeterminate="true" />

    </LinearLayout>
</ScrollView>